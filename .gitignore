# Dependencies
node_modules

# Build output
dist
dist-ssr
.vite

# Local env — VITE_SITE_URL is set per-environment. See .env.example
.env
.env.local
*.local

# Editor / OS
.DS_Store
.vscode
.idea

# Tooling scratch
.playwright-mcp
.agents
.claude
.omo
.vercel

# Visual-QA captures and the harness that produces them — reference shots taken
# at breakpoint review, not assets the site ships.
/*.png
public/__shot.html

# Source résumé (the shipped copy lives in public/)
Resume:CV.pdf
Deepanshu_Operations.pdf

# Redesign source material: the CV of record, the visual references and the
# brief. Carries a phone number, and the reference screenshots are someone
# else's work - neither belongs in a public repository. content.ts cites this
# folder as its source, so keep it locally.
Reference/

# The public origin this site is deployed to. Feeds the canonical URL,
# OpenGraph tags, JSON-LD, robots.txt and sitemap.xml from this one place.
# No trailing slash. The build fails fast if this is unset.
VITE_SITE_URL=https://example.com
